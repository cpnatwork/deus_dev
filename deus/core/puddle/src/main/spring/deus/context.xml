<?xml version="1.0" encoding="UTF-8"?>
<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd">

	
	<!-- this is needed for some annotations used to configure the application context to be recognized
	among are the following annotations:
	
	Dependency Injection:
	@Required
	@Resource (JSR-250)
	@Autowired (with @Qualifier)
	
	Initialization/Destruction callbacks:
	@PostConstruct
	@PreDestroy
	
	more help: http://static.springframework.org/spring/docs/2.5.x/reference/beans.html#beans-annotation-config -->
	<context:annotation-config />
	
	<!-- 
	the aforementioned annotations require the bean to already be present in the application context. If you do not like
	to instantiate the bean in XML using <bean>, use stereotype annotations to annotate the spring managed components:
			
	stereotypes for spring managed components:
	@Component
	 @Controller
	 @Service
	 @Repository
	
	more help: http://static.springframework.org/spring/docs/2.5.x/reference/beans.html#beans-classpath-scanning
	 -->
	<context:component-scan base-package="deus.core.soul"/>
	
	
	<!-- this is needed for the @Configurable annotations to be recognized and the annotated classes to be injected the
	dependencies either configured by the related prototype-scoped bean or configured by @Autowired annotations.
	see http://static.springframework.org/spring/docs/2.5.x/reference/aop.html#aop-atconfigurable for more  -->
	<context:spring-configured />
	
	
	<!-- this switches on the load-time weaving 
	remember to use -javaagent:/PATH/TO/spring-agent-2.5.6.jar as a java VM argument, when starting the application!
	see more here: http://static.springframework.org/spring/docs/2.5.x/reference/aop.html#aop-aj-ltw
	-->
    <context:load-time-weaver aspectj-weaving="autodetect" />
	
</beans>
